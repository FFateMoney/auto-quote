# Excel 读取协议设计文档

## 1. 背景与目标

本协议用于支持 LLM（大语言模型）以**受控、有限、结构化**的方式探索任意 Excel 文档。模型只负责"决定下一步看什么"，由本协议提供的函数负责安全地返回局部内容。

设计原则：

1. **极简函数集**：对外仅暴露三个读取函数 `get_rows`、`get_columns`、`get_cell`。
2. **合并单元格对模型透明**：模型不需要理解 Excel 的合并机制；所有合并复杂度封装在 `Cell` 的元数据中。
3. **去重**：同一个合并块在同一次扫描调用中只出现一次。
4. **一致的身份**：合并块无论从哪个位置访问，其 `range` 字段始终是同一个标识符。
5. **A1 notation 优先**：所有坐标和范围使用 Excel 标准的 A1 表示（列字母+行数字），与 Excel 界面显示一致。

---

## 2. 核心数据类型：`Cell`

所有读取函数返回的最小单元都是 `Cell`。

### 2.1 字段定义

| 字段 | 类型 | 说明 |
|---|---|---|
| `range` | `str` | A1 notation。普通单元格如 `"B5"`；合并块如 `"B8:I8"`。**这是 Cell 的唯一身份标识。** |
| `value` | `Any` | 单元格的值。合并块取锚点（左上角）的值。空单元格为 `None`。 |
| `is_merged` | `bool` | 是否为合并块。可由 `range` 是否含 `":"` 推断，冗余字段是为方便模型直接读取。 |
| `rowspan` | `int` | 跨越的行数。普通单元格为 `1`。 |
| `colspan` | `int` | 跨越的列数。普通单元格为 `1`。 |

### 2.2 推荐的紧凑序列化格式

为节省 token，对外（送给模型时）建议使用缩写字段名：

```json
{"r": "B8:I8", "v": "Test Method...", "rs": 1, "cs": 8}
```

普通单元格可省略 `rs` 和 `cs`（默认 1）：

```json
{"r": "B5", "v": "Big Game Hunt"}
```

完全空的普通单元格可省略 `v`：

```json
{"r": "A8"}
```

具体序列化策略由调用方决定，但内部 `Cell` 对象始终保留全字段。

---

## 3. 合并单元格的核心规则

这是本协议最关键的设计决策，所有函数行为都建立在这条规则之上。

### 3.1 锚点归属规则

> **每个合并块归属于其锚点（左上角单元格）所在的行 和 锚点所在的列。**

由此推出：

- `get_rows(start, end)` 中，合并块**仅**当锚点行在 `[start, end]` 区间内时返回。
- `get_columns(start, end)` 中，合并块**仅**当锚点列在 `[start, end]` 区间内时返回。
- `get_cell(address)` 中，无论 `address` 指向合并块的哪个位置（锚点或非锚点），都返回该合并块的完整 `Cell` 对象。

### 3.2 三个函数的语义区分

| 函数 | 语义 | 合并块行为 |
|---|---|---|
| `get_rows` | 行扫描 | 按锚点归属，零重复 |
| `get_columns` | 列扫描 | 按锚点归属，零重复 |
| `get_cell` | 坐标指向 | 命中合并块的任意位置都返回完整块 |

### 3.3 跨行又跨列的合并块

按统一规则处理。例如合并块 `B2:D4`（锚点 `B2`）：

- `get_rows(2, 2)` → 返回此合并块
- `get_rows(3, 3)`、`get_rows(4, 4)` → 不返回（锚点不在 3、4 行）
- `get_columns("B", "B")` → 返回此合并块
- `get_columns("C", "D")` → 不返回（锚点列是 B）
- `get_cell("C3")` → 返回此合并块（range = "B2:D4"）

### 3.4 空锚点的合并块

锚点 `value` 为 `None` 的合并块**必须**正常返回，不可吞掉。

理由：合并块的"形状"本身就是结构信息（占位、分组、未填写的区块）。返回空合并块能让模型识别出表格的占位结构。

返回示例：

```json
{"r": "B4:D4", "v": null, "rs": 1, "cs": 3}
```

---

## 4. 函数规范

### 4.1 `get_rows(start, end)`

**签名**

```python
def get_rows(start: int, end: int) -> list[Cell]
```

**参数**

- `start`：起始行号，1-based 整数（与 Excel 显示一致）。
- `end`：结束行号，**包含**（inclusive），1-based 整数。
- 要求 `start <= end`，且 `start >= 1`。

**返回值**

返回 `[start, end]` 区间内所有行的 Cell 列表。

**排序规则**

返回列表按以下顺序：

1. 主序：行号升序（按 Cell 的最小行号，即合并块的锚点行）。
2. 次序：同一行内按列号升序（按 Cell 的最小列号）。

**单行返回规则**

对某一行 `r`，返回该行中：

- 所有锚点在第 `r` 行的合并块（包含跨多列、跨多行的）。
- 所有第 `r` 行的普通单元格，**前提是**该位置不被任何合并块覆盖。

> 注意：如果某个普通单元格的位置被一个跨列合并块覆盖（即使该合并块锚点不在本行），它**不应**单独返回。这种情况下，第 `r` 行的"该列位置"是被合并块占据的（合并块会在锚点所在行返回）。

**空单元格的处理**

- 完全空的普通单元格（`value is None`）：**返回**，但 `v` 字段为 `null`。
- 完全空的合并块（锚点 `value is None`）：**返回**（见 3.4）。

实现者可选择性提供一个 `skip_empty` 参数让调用方关闭空单元格返回，但默认应返回（避免模型误以为整行为空）。

**示例**（基于本文末尾的 Vibration Testing 表）

```python
get_rows(8, 8)
# 返回：
# [
#   Cell(range="A8", value=None, is_merged=False, rowspan=1, colspan=1),
#   Cell(range="B8:I8", value="Test Method...", is_merged=True, rowspan=1, colspan=8),
# ]

get_rows(3, 3)
# 返回：
# [
#   Cell(range="A3", value=None),
#   Cell(range="B3:D3", value="英寸：长45.0''*宽19.25''..."),
#   Cell(range="E3:G3", value="英寸：长55.5''*宽18''..."),
#   Cell(range="H3:I3", value="英寸：长55.5''*宽13.5''..."),
# ]
```

---

### 4.2 `get_columns(start, end)`

**签名**

```python
def get_columns(start: str, end: str) -> list[Cell]
```

**参数**

- `start`：起始列字母（如 `"A"`、`"AA"`），大小写不敏感（实现时内部统一转大写）。
- `end`：结束列字母，**包含**（inclusive）。
- 要求列字母代表的列号满足 `start <= end`。

> 设计提示：是否同时接受整数列号（如 `2` 代表 B 列）由实现者决定。为协议简洁，**推荐只接受字母**。

**返回值**

返回 `[start, end]` 区间内所有列的 Cell 列表。

**排序规则**

1. 主序：列号升序（按 Cell 的最小列号）。
2. 次序：同一列内按行号升序（按 Cell 的最小行号）。

**单列返回规则**

对某一列 `c`，返回该列中：

- 所有锚点在第 `c` 列的合并块（包含跨多列、跨多行的）。
- 所有第 `c` 列的普通单元格，**前提是**该位置不被任何合并块覆盖。

**示例**

```python
get_columns("B", "B")
# 返回 B 列的所有内容。注意：A1:I1 锚点在 A，不在 B，所以不出现。
# [
#   Cell(range="B2:D2", value="Comeback\n双弓"),
#   Cell(range="B3:D3", value="英寸：长45.0''..."),
#   Cell(range="B4:D4", value=None),
#   Cell(range="B5", value=None),       # 第5行 B 列单独存在（合并是 C5:D5）
#   Cell(range="B6", value=None),
#   Cell(range="B7:D7", value=None),
#   Cell(range="B8:I8", value="Test Method..."),
#   Cell(range="B9:I9", value="No loss of latch function..."),
#   Cell(range="B10:I10", value="1.In addition to..."),
#   Cell(range="B11:I11", value="袁伟180..."),
# ]
```

---

### 4.3 `get_cell(address)`

**签名**

```python
def get_cell(address: str) -> Cell
```

**参数**

- `address`：单个单元格的 A1 地址，如 `"B5"`、`"E8"`。**不接受范围**（如 `"B5:C5"`）——范围请用 `get_rows` 或 `get_columns`。

**返回值**

返回一个 `Cell` 对象：

- 如果 `address` 指向一个不在任何合并块内的普通单元格，返回该单元格本身。
- 如果 `address` 指向某个合并块（无论是否锚点位置），返回该**合并块的完整 Cell 对象**，其 `range` 字段为合并块的完整范围，而非 `address`。

**关键示例**

```python
get_cell("B8")  # 锚点位置
get_cell("E8")  # 合并块内部
get_cell("I8")  # 合并块末端
# 三次调用返回的 Cell 对象身份相同：
# Cell(range="B8:I8", value="Test Method...", is_merged=True, rowspan=1, colspan=8)
```

这条规则保证了模型多次访问同一个合并块时，能通过 `range` 字段识别出"这是同一块东西"。

**空单元格**

如果 `address` 指向一个不在合并块内的空单元格，返回：

```python
Cell(range="B5", value=None, is_merged=False, rowspan=1, colspan=1)
```

不要返回 `None` 或抛错。

---

## 5. 错误处理

### 5.1 应当抛错的情况

| 场景 | 异常类型建议 |
|---|---|
| `address` 格式非法（如 `"5B"`、`"B"`、`""`） | `ValueError` |
| 列字母含非法字符（如 `"B1"`、`"A!"`） | `ValueError` |
| `get_cell` 传入了范围（如 `"B5:C5"`） | `ValueError` |
| `start > end` | `ValueError` |
| `start < 1` 或列字母代表的列号 < 1 | `ValueError` |
| `sheet` 不存在（如果有 sheet 上下文） | `KeyError` |

错误信息应包含**实际值 + 期望格式 + 当前 sheet 的合法边界**，便于模型自我修正。例如：

```
ValueError: row 0 is invalid (rows are 1-based). Valid range for current sheet: 1..11
```

### 5.2 越界的宽容处理

为提升模型探索的容错性：

- `get_rows(5, 100)` 当 sheet 最大行数为 `11` 时：返回第 5~11 行，不抛错。
- `get_rows(50, 100)` 完全越界时：返回空列表 `[]`，不抛错。
- `get_columns` 同理。
- `get_cell("Z99")` 完全越界时：抛 `ValueError`，因为这是明确的"指向"语义，越界一定是错误。

> 设计权衡：扫描函数容忍越界（截断），指向函数严格越界检查（报错）。

---

## 6. Sheet 上下文

本协议三个函数**不包含** sheet 参数，假定调用方维护"当前 sheet"上下文。建议在协议之外提供：

```python
list_sheets() -> list[str]
set_current_sheet(name: str) -> None
get_current_sheet() -> str
```

或者，实现者可选择给三个函数都加上可选的 `sheet` 参数：

```python
def get_rows(start: int, end: int, sheet: str | None = None) -> list[Cell]
```

具体由项目架构决定，但不应让模型在每次调用时都被迫指定 sheet（增加 token 和出错概率）。

---

## 7. 实现提示

### 7.1 推荐使用 openpyxl

```python
from openpyxl import load_workbook
wb = load_workbook(path, data_only=True)  # data_only=True 让公式返回缓存值
```

`data_only=True` 很重要——否则公式单元格会返回公式字符串而非结果。

### 7.2 合并块的快速查询

`openpyxl` 的 `ws.merged_cells.ranges` 是一个列表，查询某个 `(row, col)` 是否在某合并块内需要遍历。对合并块多的 sheet 应建立索引：

```python
class MergeIndex:
    def __init__(self, ws):
        # 字典：(row, col) -> MergedCellRange
        self._map = {}
        for mr in ws.merged_cells.ranges:
            for r in range(mr.min_row, mr.max_row + 1):
                for c in range(mr.min_col, mr.max_col + 1):
                    self._map[(r, c)] = mr

    def find(self, row, col):
        return self._map.get((row, col))
```

对每个 sheet 构建一次，后续 O(1) 查询。

### 7.3 列字母与列号的转换

```python
from openpyxl.utils import get_column_letter, column_index_from_string

column_index_from_string("B")   # 2
get_column_letter(2)            # "B"
get_column_letter(27)           # "AA"
```

### 7.4 构建 Cell 的辅助函数

```python
def build_cell(ws, merge_index, row, col) -> Cell:
    """构造 (row, col) 位置对应的 Cell 对象。"""
    mr = merge_index.find(row, col)
    if mr is not None:
        anchor_value = ws.cell(row=mr.min_row, column=mr.min_col).value
        range_str = f"{get_column_letter(mr.min_col)}{mr.min_row}:{get_column_letter(mr.max_col)}{mr.max_row}"
        return Cell(
            range=range_str,
            value=anchor_value,
            is_merged=True,
            rowspan=mr.max_row - mr.min_row + 1,
            colspan=mr.max_col - mr.min_col + 1,
        )
    else:
        v = ws.cell(row=row, column=col).value
        return Cell(
            range=f"{get_column_letter(col)}{row}",
            value=v,
            is_merged=False,
            rowspan=1,
            colspan=1,
        )
```

### 7.5 扫描去重的实现思路

`get_rows` 的实现伪代码：

```python
def get_rows(start, end):
    result = []
    seen_merges = set()  # 记录已加入的合并块（按 range 字符串去重）
    for r in range(start, min(end, ws.max_row) + 1):
        for c in range(1, ws.max_column + 1):
            mr = merge_index.find(r, c)
            if mr is not None:
                # 锚点归属规则：仅当锚点行 == 当前行时才加入
                if mr.min_row != r:
                    continue
                cell = build_cell(ws, merge_index, mr.min_row, mr.min_col)
                if cell.range not in seen_merges:
                    seen_merges.add(cell.range)
                    result.append(cell)
            else:
                cell = build_cell(ws, merge_index, r, c)
                result.append(cell)
    return result
```

`get_columns` 镜像处理（外层循环列，内层循环行，锚点列归属判断）。

### 7.6 数据类型处理

- 公式：使用 `data_only=True` 加载后读到的是缓存值；如缓存为 `None`（文件未在 Excel 中保存过），可考虑回退到读取公式字符串。
- 日期：`openpyxl` 自动返回 `datetime` 对象。可在序列化时统一格式化为 ISO 字符串。
- 富文本：`openpyxl` 通常返回纯文本部分，富文本格式信息会丢失。如果项目需要保留格式，需特殊处理（不在本协议范围内）。

---

## 8. 完整示例：Vibration Testing 表

### 8.1 表的实际结构

参考文件：`Vibration_Test振动测试.xlsx`，仅一个 sheet 名为 ` Vibration Testing`（注意前有空格）。

实际尺寸 `A1:I11`，共 11 行 9 列，包含 21 个合并块。

合并块清单（按行号排序）：

| 范围 | 锚点值（摘要） |
|---|---|
| `A1:I1` | "Vibration Testing Require" |
| `B2:D2` | "Comeback / 双弓" |
| `E2:G2` | "Clearwater / 双枪" |
| `H2:I2` | "Creedence / 单枪" |
| `B3:D3` | 尺寸（产品 1） |
| `E3:G3` | 尺寸（产品 2） |
| `H3:I3` | 尺寸（产品 3） |
| `B4:D4` | None（空合并块） |
| `E4:G4` | None |
| `H4:I4` | None |
| `C5:D5` | "Big Game Hunt / 橄榄绿" |
| `F5:G5` | "Big Game Hunt / 橄榄绿" |
| `C6:D6` | 1 |
| `F6:G6` | 1 |
| `B7:D7` | None |
| `E7:G7` | None |
| `H7:I7` | None |
| `B8:I8` | "Test Method..." 大段说明 |
| `B9:I9` | "No loss of latch function..." |
| `B10:I10` | "1.In addition to..." |
| `B11:I11` | "袁伟180..." 联系方式 |

### 8.2 调用示例

**Case 1：扫描表头行**

```python
get_rows(2, 2)
# →
# [
#   Cell("A2", None),
#   Cell("B2:D2", "Comeback\n双弓", merged, rs=1, cs=3),
#   Cell("E2:G2", "Clearwater\n双枪", merged, rs=1, cs=3),
#   Cell("H2:I2", "Creedence\n单枪", merged, rs=1, cs=2),
# ]
```

**Case 2：扫描大段说明所在行**

```python
get_rows(8, 8)
# →
# [
#   Cell("A8", None),
#   Cell("B8:I8", "Test Method...", merged, rs=1, cs=8),
# ]
```

**Case 3：扫描混合区域**

```python
get_rows(5, 6)
# →
# 第5行：
#   Cell("A5", None),
#   Cell("B5", None),                     # B5 不在合并块内，独立单元格
#   Cell("C5:D5", "Big Game Hunt..."),
#   Cell("E5", None),
#   Cell("F5:G5", "Big Game Hunt..."),
#   Cell("H5", None), Cell("I5", None),
# 第6行：
#   Cell("A6", None),
#   Cell("B6", None),
#   Cell("C6:D6", 1),
#   Cell("E6", None),
#   Cell("F6:G6", 1),
#   Cell("H6", None), Cell("I6", None),
```

**Case 4：列扫描，看跨大合并块不重复**

```python
get_columns("C", "E")
# →
# C 列：所有锚点列在 C 的 cell
#   Cell("C5:D5", ...), Cell("C6:D6", 1),
#   还有 C1 ~ C11 中不被任何合并块覆盖的普通单元格
#   注意：C8 在 B8:I8 内但锚点是 B，不出现在 C 列结果里
# D 列：D 列没有任何锚点列为 D 的合并块（B2:D2 锚点是 B；C5:D5 锚点是 C）
#   只返回 D 列中不被合并块覆盖的普通单元格（基本都是空）
# E 列：
#   Cell("E2:G2", ...), Cell("E3:G3", ...), Cell("E4:G4", None), Cell("E7:G7", None),
#   还有 E 列中不被合并覆盖的普通单元格
```

**Case 5：指向合并块的非锚点位置**

```python
get_cell("E8")
# → Cell(range="B8:I8", value="Test Method...", is_merged=True, rs=1, cs=8)
#   range 字段是合并块的真实范围 "B8:I8"，不是 "E8"

get_cell("B8")
# → Cell(range="B8:I8", ...)  # 同上，身份相同
```

---

## 9. 测试用例建议

实现完成后，至少应通过以下单元测试：

1. **单行无合并**：构造一个 5×5 无合并的 sheet，`get_rows(1,1)` 返回 5 个 Cell。
2. **单行多个合并**：用本文档的第 3 行，验证返回 4 个 Cell（A3 普通 + 3 个合并块）。
3. **跨列大合并**：用本文档的第 8 行，验证返回 2 个 Cell（A8 普通 + B8:I8 合并块）。
4. **跨行合并的锚点归属**：构造 `B2:C4` 合并块，验证 `get_rows(2,2)` 返回此块，`get_rows(3,3)` 和 `get_rows(4,4)` 不返回。
5. **列扫描去重**：用本文档 `get_columns("B","B")`，验证 B8:I8 出现 1 次，且 A1:I1 不出现（锚点在 A）。
6. **get_cell 非锚点身份一致**：`get_cell("B8")` 和 `get_cell("E8")` 和 `get_cell("I8")` 返回的 Cell 对象 `range` 字段都为 `"B8:I8"`。
7. **空锚点合并块返回**：用本文档第 4 行，验证返回 4 个 Cell（A4 + 三个 value 为 None 的合并块）。
8. **越界宽容**：`get_rows(5, 100)` 在 11 行的 sheet 上返回第 5~11 行，不抛错。
9. **完全越界**：`get_rows(100, 200)` 返回空列表。
10. **指向越界报错**：`get_cell("Z99")` 抛 `ValueError`。
11. **非法地址报错**：`get_cell("5B")`、`get_cell("B5:C5")`、`get_cell("")` 均抛 `ValueError`。

---

## 10. 不在本文档范围内

以下功能后续可能扩展，但**不属于本协议**：

- 切分函数（如 `create(cells)`、`create(rows, columns)`）：另文档定义。
- sheet 管理（`list_sheets`、`set_current_sheet`）：辅助接口，由调用方架构决定。
- 样式信息（字体、底色、边框）：可作为 `Cell` 的可选扩展字段（如 `style`），但默认协议不包含。
- 公式表达式：默认使用 `data_only=True` 仅返回缓存值。
- 表格区域识别（region detection）：上层逻辑，不在读取协议层。

---

## 附录 A：术语表

| 术语 | 含义 |
|---|---|
| 合并块 / merged range | 由多个物理单元格合并成的视觉一体的区域，如 `B8:I8` |
| 锚点 / anchor | 合并块的左上角单元格，是该合并块唯一持有 `value` 的位置 |
| 普通单元格 | 不在任何合并块内的单元格，`rowspan == colspan == 1` |
| A1 notation | Excel 标准坐标表示法，列字母在前、行数字在后，如 `B5`、`AA12` |
| 锚点归属规则 | 本协议核心规则：合并块归属于锚点的行和锚点的列 |